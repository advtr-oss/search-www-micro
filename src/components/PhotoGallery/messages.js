import { defineMessages } from 'react-intl'

export const scope = 'app.components.PhotoGallery'

export default defineMessages({
  watermark: {
    id: `${scope}.watermark`,
    defaultMessage: 'Photos provided by'
  }
})
