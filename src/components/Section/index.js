/**
 * Section.js
 *
 * Simple <section> wrapper, might not be generic enough and
 * gets moved to `src/containers/DetailCard/Shared/Section.js`
 * */

import styled from 'styled-components'

const Section = styled.section`
  padding: 15px 20px;
  border-bottom: 1px solid #d3dae0;
`

export default Section
