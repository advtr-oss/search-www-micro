import styled from 'styled-components'

import Glow from '../Glow'

const GlowIcon = styled(Glow)`
  width: 24px;
  height: 24px;
`

const GlowText = styled(Glow)`
  height: 24px;
  width: 100%
`

export { GlowIcon, GlowText }
