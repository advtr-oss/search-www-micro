/**
 * Card.js
 *
 * Card to hold any data shown on screen
 *
 * */

import styled from 'styled-components'

const Card = styled.div`
  background-color: #fff;
  box-shadow: 0 0 20px 0 rgba(0,0,0,.3);
  border-radius: 10px;
  pointer-events: all;
  margin-bottom: 15px;
  display: flex;
`

export default Card
